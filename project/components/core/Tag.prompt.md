Small mono pill for clients, categories, and capability chips.

```jsx
<Tag>Media Facade</Tag>
<Tag variant="solid">2026</Tag>
<Tag variant="ghost">Projection Mapping</Tag>
```

Variants: `outline` (default), `solid` (champagne), `ghost` (faint surface).
