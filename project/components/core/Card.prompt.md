Base surface container — hairline border on a raised ink surface.

```jsx
<Card hover>
  <Eyebrow>Capability</Eyebrow>
  <p>Media facade engineering at landmark scale.</p>
</Card>
```

Props: `hover` (lift + brighten), `padded` (default true). Compose Stat, Eyebrow, etc. inside.
