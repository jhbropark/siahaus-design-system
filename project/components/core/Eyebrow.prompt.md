The signature mono kicker that sits above nearly every title in the system.

```jsx
<Eyebrow marker>Case Study — 2026</Eyebrow>
<Eyebrow color="var(--text-subtle)">Executive Summary</Eyebrow>
```

Always uppercase + wide tracking. Use `marker` for the `//` prefix on section openers.
